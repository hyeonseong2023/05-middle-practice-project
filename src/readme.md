Use the starting project attached to this lecture
Add components for display a header, fetching user input & outputting the results table
Fetch & store user input(i.e., the entered investment parameters)
Derive investment results with help of the provided utility function (in the starting project)
Display investment results in a HTML table (use <table>, <thead>, <tr>, <th>, <td>)
Conditionally display on info message if an invalid duration (<1) was entered>

---

이 강의에 **첨부된 시작 프로젝트(starting project)**를 사용하세요.

헤더를 표시하는 컴포넌트, 사용자 입력을 받는 컴포넌트, 결과 테이블을 출력하는 컴포넌트를 추가하세요.

사용자가 입력한 값(즉, 투자 관련 파라미터)을 가져와서 상태로 저장하세요.

시작 프로젝트에 제공된 유틸리티 함수를 사용해 투자 결과를 계산(derive) 하세요.

계산된 투자 결과를 HTML 테이블로 표시하세요
(<table>, <thead>, <tr>, <th>, <td> 사용)

입력된 기간(duration)이 1 미만일 경우,
안내 메시지(info message) 를 조건부로 표시하세요.
